UPDATE "ORDER" o
SET SYMBOL = (
    SELECT SYMBOL
    FROM STOCK s
    WHERE s.CORP_ID IN (
        SELECT c.CORP_ID
        FROM CORPORATION c
        WHERE c.SECTOR = o.Sector
    )
    ORDER BY DBMS_RANDOM.VALUE
    FETCH FIRST 1 ROWS ONLY
);