DECLARE
	VAR DECIMAL(10,2);
BEGIN 
	FOR R IN(SELECT * FROM ADMIN)
	LOOP 
		IF R.ADDER_ID = 1000 THEN 
			VAR := 20000;
		ELSIF R.ADDER_ID = 1010 THEN 
			VAR := 10000;
		ELSE 
			VAR := 5000;
		END IF;
		UPDATE ADMIN 
		SET FUNDS = VAR 
		WHERE ADMIN_ID = R.ADMIN_ID;
		
	END LOOP;
END;
/